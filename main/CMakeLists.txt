file(GLOB_RECURSE srcs "main.c" "src/*.c")

idf_component_register(SRCS "${srcs}"
                       PRIV_REQUIRES esp_driver_gpio esp_driver_spi esp_driver_i2c esp_adc
                       INCLUDE_DIRS "./include")
